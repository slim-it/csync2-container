# csync2-container

Minimal csync2 image for `slim-it/flux-infra`.

The image uses the Ubuntu packaged csync2 build and publishes to:

```text
ghcr.io/slim-it/csync2-container:2.0.42.4          # csync2 version, Renovate-orderable
ghcr.io/slim-it/csync2-container:<csync2-version>-<commit>
```
